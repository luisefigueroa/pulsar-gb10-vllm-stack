# Qwen3-1.7B split TP=2 across both nodes. This draft recipe is retained for
# future two-rank onboarding; it is not a reviewed or tested configuration.
MODEL="Qwen/Qwen3-1.7B"
SERVED_NAME="qwen3-1.7b-2node"
PROFILE_FAMILY="qwen3-1.7b"
VARIANT_LABEL="2-node draft recipe"
PROFILE_PURPOSE="diagnostic"
NODES=2
TOPOLOGY_CLASS="roce-full-mesh"
MIN_RAILS_PER_PAIR=2
PORT=8000
GPU_MEM_UTIL=0.30
ENGINE_ARGS=(
  --max-model-len 32768
  --max-num-seqs 16
  --tensor-parallel-size 2
  --distributed-executor-backend mp
)
STATUS="untested"
NOTES="Untested two-rank onboarding recipe. No reviewed identity, no MODEL_SERVING_RELEASE_ID, and not a serving recommendation."
